# Use /.*/ for any origin, or comma-separated URLs (no trailing slash on URLs)
STORE_CORS=/.*/
ADMIN_CORS=/.*/
AUTH_CORS=/.*/
REDIS_URL=redis://localhost:6379
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
DATABASE_URL=
DB_NAME=medusa-v2
